import React from 'react';
interface SystemMetricsProps {
    refreshInterval?: number;
}
export default function SystemMetrics({ refreshInterval }: SystemMetricsProps): React.JSX.Element;
export {};
